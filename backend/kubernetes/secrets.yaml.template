# secrets.yaml - Template only, do not commit actual secrets!
# Use: kubectl create secret generic blogify-secrets --from-literal=google-api-key=XXX ...
apiVersion: v1
kind: Secret
metadata:
  name: blogify-secrets
type: Opaque
stringData:
  # Replace with actual values or use kubectl create secret
  google-api-key: "REPLACE_ME"
  tavily-api-key: "REPLACE_ME"
  database-url: "postgresql+asyncpg://user:pass@host:5432/db"
